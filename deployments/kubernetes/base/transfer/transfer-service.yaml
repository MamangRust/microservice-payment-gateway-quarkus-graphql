apiVersion: v1
kind: Service
metadata:
  name: transfer
  namespace: payment-gateway
spec:
  selector:
    app: transfer
  ports:
    - name: http
      port: 8090
      targetPort: 8090
    - name: grpc
      port: 9010
      targetPort: 9010
  type: ClusterIP
