apiVersion: v1
kind: Service
metadata:
  name: role
  namespace: payment-gateway
spec:
  selector:
    app: role
  ports:
    - name: http-metrics
      port: 8086
      targetPort: 8086
    - name: grpc
      port: 9006
      targetPort: 9006
  type: ClusterIP
